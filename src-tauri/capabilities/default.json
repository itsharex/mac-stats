{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main and CPU webview windows: core IPC, shell open URLs, custom commands",
  "windows": ["main", "cpu"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
