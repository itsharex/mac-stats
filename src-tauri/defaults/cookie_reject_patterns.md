# Cookie reject / consent-dismiss patterns (one per line)
# After BROWSER_NAVIGATE, mac-stats looks for a button/link whose text contains any of these
# strings (case-insensitive) and clicks it to dismiss the cookie banner.
# Add or translate patterns for your language. Lines starting with # are ignored.
# Examples: "Reject all", "Only necessary", "Ablehnen", "Nur notwendige", "Refuser", etc.

ablehnen
reject all
reject cookies
reject
nur notwendige
only necessary
necessary cookies only
alle ablehnen
decline
no thanks
nein, danke
nur essenzielle
essential only
save preferences
speichern
akzeptieren nur notwendige
accept necessary only
